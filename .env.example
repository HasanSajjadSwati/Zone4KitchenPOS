# POS System Configuration

# Logging Configuration
# Set DEBUG to 'true' to enable console logging, 'false' to disable
VITE_DEBUG=false

# Backend API Configuration
# Use /api when the frontend is served by the backend (recommended for production)
# Use http://localhost:3001/api for local development when running Vite separately
VITE_API_URL=/api
